def digits_in_integer_is_four(number):
	if number < 10000:
		number = True
	return number		
	
	def first_digit_in_integer(first, number):
		first = number // 1000
		return x
	
	def second_digit_in_integer(second, number):
		
			



}
}