import {expect, test} from 'vitest';

test('test to Add', () => {
    const result = 1 + 1;
    expect(result).toBe(2)
})

test('test to Subtract', () => {
    const result = 5 - 3;
    expect(result).toBe(2)
})
