function combineMyArray(array1, array2){
	let newArray = []
	let count = 0;
	let counter = 0;
	let newValue = 0;
for (count = 0; count < array1.length; count++){
newValue(newArray.length) = array1[count];
for (counter = 0; counter < array2.length; counter++){
newValue(newArray.length) = array2[counter];
return newValue;
}
}
}
let array1 = [2, 3, 3, 4, 5];
let array2 = [3, 4, 5, 6, 7];
console.log(array1, array2)